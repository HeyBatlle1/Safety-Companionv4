from sqlalchemy import MetaData
from app.core.database import Base

# Define schemas for organization
# Define schemas for organization
APP_SCHEMA = None  # None for SQLite compatibility
OSHA_SCHEMA = None

metadata = MetaData()