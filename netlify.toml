[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[secrets]
  omit_paths = [".next/**", "middleware.ts"]
  omit_keys = ["NEXT_PUBLIC_*"]
