[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/.next"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = ".next/**,middleware.ts"
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_*"
